@model Domain.Entities.Procedure
@{
    ViewBag.Title = Model.Name;
}
<div>
    <dl class="dl-horizontal">
        <dt>Идентификатор</dt>
        <dd>@Model.Id</dd>

        <dt>Название</dt>
        <dd>@Model.Name</dd>

        <dt>Цена</dt>
        <dd>@Model.Price</dd>
    </dl>
</div>